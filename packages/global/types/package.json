{
  "name": "@global/types",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "setup": "tsup --clean --silent",
    "dev": "tsup --watch"
  },
  "devDependencies": {
    "@tooling/tsconfigs": "workspace:*",
    "@tooling/tsup": "workspace:*",
    "tsup": "8.4.0"
  }
}
